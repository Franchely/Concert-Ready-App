import React, {Component} from "react"

class Setlist extends Component {

   

    render() {

        return (

            <div className="setlist">
                {/* <h3>{this.props.setlist.artist}</h3>
                <h4>{this.props.setlist.date}</h4>
                <h4>{this.props.setlist.venue}</h4>
                <h4>{this.props.setlist.city}, {this.props.setlist.country}</h4>
                <ol className="songs-list">
                    {this.renderSongs()}
                </ol>
                <button onClick={this.handleSave} className="save-setlist">Save Setlist</button> */}
            </div>
        )
    }
}

export default Setlist